# ------------------------------------------------Environment Settings------------------------------------------------#
gpu_id: "0"

# -------------------------------------------------Retrieval Settings------------------------------------------------#
# If set the name, the model path will be find in global paths
retrieval_method: "../../assets/embedding_model/e5-base-v2"  # name or path of the retrieval model. 
index_path: "../../assets/indexes/wiki18_100w_e5_flat_inner.index" # path to the indexed file
faiss_gpu: False # whether use gpu to hold index
corpus_path: "../../assets/corpus/wiki18_100w.jsonl"  # path to corpus in '.jsonl' format that store the documents